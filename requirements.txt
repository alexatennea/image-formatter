# Runtime dependencies, pinned exactly (including transitive ones) so every
# build ships the same OCR stack that was tested. Unbounded ranges here have
# already bitten this project: rapidocr-onnxruntime silently ignores
# unrecognised options, so an engine upgrade can change behaviour without
# any error. Bump deliberately, re-run the tests and re-check real photos.
#
# Needs Python 3.12 exactly: numpy 2.5 requires >=3.12 and
# rapidocr-onnxruntime 1.4.4 requires <3.13.
#
# Build/test tooling lives in requirements-build.txt.

# Direct
pillow==12.3.0
rapidocr-onnxruntime==1.4.4

# Transitive (via rapidocr-onnxruntime / onnxruntime)
flatbuffers==25.12.19
numpy==2.5.3
onnxruntime==1.30.0
opencv-python==5.0.0.93
packaging==26.3
protobuf==7.36.2
pyclipper==1.4.0
PyYAML==6.0.3
shapely==2.1.2
six==1.17.0
tqdm==4.70.1

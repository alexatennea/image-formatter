# Everything needed to test and package the app, on top of the runtime pins.
# Platform-specific sub-dependencies of PyInstaller (macholib on macOS,
# pefile/pywin32-ctypes on Windows) are left to the resolver.
-r requirements.txt

pyinstaller==6.22.3
pyinstaller-hooks-contrib==2026.7
pytest==9.1.1
